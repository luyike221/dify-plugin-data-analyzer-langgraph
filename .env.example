#########################################################################################
# Dify Plugin 远程调试配置
# 用于在开发时连接到 Dify 平台进行插件调试
#########################################################################################

# 安装方式：remote 表示远程调试模式
INSTALL_METHOD=remote

# 远程调试地址：从 Dify 管理界面获取
# 格式：host:port（例如：debug.dify.ai:5003 或 localhost:5003）
REMOTE_INSTALL_URL=debug.dify.ai:5003

# 远程调试密钥：从 Dify 管理界面获取
# 这是用于验证插件调试连接的密钥
REMOTE_INSTALL_KEY=your-debug-key-here

#########################################################################################
# Provider 配置（可选 - 用于本地开发测试）
# 注意：在生产环境中，这些配置应该在 Dify UI 中通过 Provider 凭据配置
# 以下配置仅用于本地开发或调试时的默认值
#########################################################################################

# LLM API 配置（可选，用于表头智能验证）
# 如果留空，将仅使用基于规则的分析
EXCEL_LLM_API_KEY=
EXCEL_LLM_BASE_URL=https://api.openai.com/v1/chat/completions
EXCEL_LLM_MODEL=gpt-4o-mini

# 数据分析 API 配置（必选，用于实际的数据分析）
# 这些配置在 Dify UI 中配置后，会通过 provider credentials 传递
# 以下仅作为本地开发时的参考
# ANALYSIS_API_URL=http://localhost:8118/v1/chat/completions
# ANALYSIS_MODEL=DeepAnalyze-8B
# ANALYSIS_API_KEY=

#########################################################################################
# 其他配置
#########################################################################################

# 最大对话轮数
MAX_ROUNDS=30

# 工作空间路径
WORKSPACE_PATH=./workspace

#########################################################################################
# 使用说明
#########################################################################################
# 1. 将此文件复制为 .env：
#    cp .env.example .env
#
# 2. 编辑 .env 文件，填入你的实际配置：
#    - 从 Dify 管理界面获取 REMOTE_INSTALL_URL 和 REMOTE_INSTALL_KEY
#    - 根据需要配置 LLM API 相关参数（可选）
#
# 3. 启动插件调试：
#    python -m main
#
# 4. 在 Dify UI 中配置 Provider 凭据：
#    - 登录 Dify 管理界面
#    - 进入插件管理，找到你的插件（标记为"调试中"）
#    - 配置 Provider 凭据（analysis_api_url, analysis_model 等）
#
# 注意：Provider 凭据（如 analysis_api_url, analysis_model）应该在 Dify UI 中配置，
# 而不是在 .env 文件中配置。.env 文件主要用于远程调试连接配置。
DIFY_API_KEY=app-o9Lz5kCURG0tb3qSC7oBDqN8
DIFY_API_BASE_URL=http://localhost/v1  # 可选，默认为 https://api.dify.ai